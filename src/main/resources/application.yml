spring:
  application:
    name: jagalchi-server
  datasource:
    url: jdbc:mysql://localhost:3306/jagalchi_node?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: jagalchi
    password: jagalchi123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: jagalchi
    password: jagalchi123
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8082
