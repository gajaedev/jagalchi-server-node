spring:
  application:
    name: jagalchi-server
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/jagalchi_node?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: jagalchi
    password: jagalchi123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq.jagalchi.local}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:jagalchi}
    password: ${RABBITMQ_PASSWORD:jagalchi123}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

server:
  port: 8082
